<%= div_for message do %>
	<div class="comment-box"><p><%= link_to("#{message.user.username}", message.user) %> said <%= time_ago_in_words(message.created_at) %> ago:</p>
	<p><%= message.message %></p></div>
<% end %>